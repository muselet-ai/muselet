name: "muselet"
description: "Lint commit messages for context (why, not just what)"
inputs:
  token:
    description: "GitHub token"
    required: false
    default: ""
runs:
  using: "node20"
  main: "dist/index.js"
